/**
 * Abstracts all customer ajax interactions
 * that consume all Customer APIs
 */

var customer = {};

customer.addToCart = function(item){
    //add item to cart and update cart
   
}


customer.removeFromCart = function (item_id) {
    //remove item id from cart and update cart
}

customer.createOrder = function (order) {
    //a new order is created after checkout
}

customer.createCustomer = function (customer) {
    //add a new customer
}