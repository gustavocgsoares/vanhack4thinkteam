/**
 * Abstracts all admin ajax interactions
 * that consume all Admin APIs
 */

var admin = {};

admin.addItemToStore = function (item) {
    //adds item to store 
}

admin.addCategories = function (category) {
    //add new Categories
}

admin.removeItemFromStore = function (itemId) {
    //remove item from store
}

admin.getOrder = function (orderId) {
    //getAll Items 
}